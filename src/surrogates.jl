"""
    optimize_surrogate(parameters, model::MultistateModel)

Optimize parameters for a Markov surrogate by minimizing the discrepancy between the prevalence curves between the two models.
"""
function optimize_surrogate(parameters, model::MultistateModel)



end
